{
  "id": "deb53e1f-ff67-4e4b-9f74-456eb6326789",
  "name": "Tool definitions on the wire",
  "kind": "other",
  "consumers": [
    "src/server.rs",
    "src/cli.rs",
    "src/client.rs",
    "tests/budgets.rs",
    "tests/http_roundtrip.rs",
    "docs/1-about/04-primitives.md",
    "README.md"
  ],
  "current": {
    "version": 1,
    "shape": {
      "budget": "tools/list and per-tool caps in tests/budgets.rs are re-measured and raised by ADR-0034; owner ranks complete tool definitions above per-session bytes",
      "guide": {
        "annotations": {
          "readOnlyHint": true
        },
        "cli": "tirith guide [TOPIC]",
        "input": {
          "agent": "string, optional",
          "topic": "optional enum: overview|claims|tasks|contracts|notices|decisions|memory|messages|lead; default overview"
        },
        "output_invalid": {
          "message": "unknown topic; lists the valid ones",
          "status": "invalid"
        },
        "output_ok": {
          "loop": "string[] ordered steps (overview)",
          "message": "one-line summary",
          "purpose": "string (overview)",
          "rules": "string[]",
          "status": "ok",
          "summary": "string (topic)",
          "tools": "overview: {group: [tool names]}; topic: [{tool, when}]",
          "topic": "string",
          "topics": "string[] (overview)"
        }
      },
      "tools_list": {
        "annotations": "every tool; notice_list is NOT read-only (unread marks notices seen)",
        "enums": "closed string params carry enum: task status, contract kind, notice kind, memory kind, guide topic",
        "outputSchema": "every tool: type object, required [status], described top-level properties; nullable fields typed [T,null] or untyped; additionalProperties allowed so lost/inbox/inbox_more/persist_error never fail validation",
        "per_parameter_descriptions": "sent (slim_schema no longer strips them); 100% coverage"
      }
    },
    "notes": "ADR-0034 (supersedes ADR-0033 in part, and the per-parameter rule of ADR-0017). Adds the 23rd tool, guide.",
    "published_by": "claude-tdqs-guide",
    "published_at": "2026-09-18T22:12:45.749211Z"
  },
  "history": []
}
