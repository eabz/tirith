{
  "site": "memory_search",
  "cases": [
    {
      "id": "smoke-memory-001",
      "difficulty": "hard",
      "why": "relevant note shares no words with the query",
      "seed": {
        "memory": [
          {"key": "m1", "by": "alice", "title": "Webhook signatures use raw body", "kind": "gotcha", "body": "Stripe HMAC verification fails if axum's Json extractor consumed the body; take Bytes first and verify before parsing.", "paths": ["src/billing/webhooks.rs"], "tags": ["stripe", "hmac"]},
          {"key": "m2", "by": "bob", "title": "Payment retries schedule", "kind": "fact", "body": "Failed card payments retry at 1d, 3d, 7d then the subscription pauses.", "paths": ["src/billing/dunning.rs"], "tags": ["billing"]},
          {"key": "m3", "by": "carol", "title": "Payment provider event ordering", "kind": "lesson", "body": "invoice.paid can arrive before invoice.created; upsert on id instead of assuming order.", "paths": ["src/billing/webhooks.rs"], "tags": ["stripe"]}
        ]
      },
      "action": {"agent": "dave", "tool": "memory_search", "args": {"query": "why does the callback from the payment processor get rejected as invalid"}},
      "expect": {"relevant": ["m1"], "partial": ["m3"]}
    },
    {
      "id": "smoke-memory-002",
      "difficulty": "easy",
      "why": "lexical match on the right note plus a lexical trap",
      "seed": {
        "memory": [
          {"key": "m1", "by": "alice", "title": "Connection pool exhaustion under load", "kind": "lesson", "body": "sqlx pool max 10 starved under the export job; export now uses its own pool of 2.", "paths": ["src/db/pool.rs"], "tags": ["db", "pool"]},
          {"key": "m2", "by": "bob", "title": "Thread pool for image resizing", "kind": "fact", "body": "rayon pool sized to physical cores for thumbnails.", "paths": ["src/media/resize.rs"], "tags": ["media"]}
        ]
      },
      "action": {"agent": "dave", "tool": "memory_search", "args": {"query": "database connection pool timeouts"}},
      "expect": {"relevant": ["m1"]}
    }
  ]
}
