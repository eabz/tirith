{
  "site": "brief",
  "cases": [
    {
      "id": "smoke-brief-001",
      "difficulty": "easy",
      "why": "one breaking signature change on the refresh path among cosmetic rows",
      "seed": {
        "notices": [
          {"key": "n1", "by": "alice", "kind": "signature", "summary": "TokenStore::refresh now async, returns Result<Token, AuthError>; callers must .await", "from": "fn refresh(&self) -> Token", "to": "async fn refresh(&self) -> Result<Token, AuthError>", "affected_paths": ["src/auth"]},
          {"key": "n2", "by": "carol", "kind": "behavior", "summary": "auth log lines now use key=value format", "from": null, "to": null, "affected_paths": ["src/auth/log.rs"]},
          {"key": "n3", "by": "dave", "kind": "rename", "summary": "renamed auth::consts::BANNER to auth::consts::LOGIN_BANNER", "from": "BANNER", "to": "LOGIN_BANNER", "affected_paths": ["src/auth/consts.rs"]}
        ],
        "contracts": [
          {"key": "c1", "by": "alice", "name": "POST /v1/token/refresh", "kind": "http", "shape": {"request": {"refresh_token": "string"}, "response": {"access_token": "string", "expires_in": "u32"}}, "consumers": ["src/auth"], "notes": "401 on revoked token"}
        ],
        "decisions": [
          {"key": "d1", "by": "alice", "title": "Refresh tokens rotate on every use", "decision": "every refresh issues a new refresh token and revokes the old one", "rationale": "replay protection", "affects_paths": ["src/auth"]},
          {"key": "d2", "by": "erin", "title": "Auth crate uses tracing, not log", "decision": "all auth logging through tracing macros", "rationale": "structured spans", "affects_paths": ["src/auth"]}
        ],
        "memory": [
          {"key": "m1", "by": "carol", "title": "Refresh race on concurrent requests", "kind": "gotcha", "body": "Two in-flight requests refreshing at once both used the old refresh token; the second got 401 after rotation. Single-flight the refresh behind a tokio Mutex.", "paths": ["src/auth/token.rs"], "tags": ["auth", "race"]},
          {"key": "m2", "by": "dave", "title": "Login page copy owner", "kind": "fact", "body": "Marketing owns banner text in src/auth/consts.rs; do not edit without a ticket.", "paths": ["src/auth/consts.rs"], "tags": ["copy"]}
        ],
        "active_agents": ["bob"]
      },
      "action": {"agent": "bob", "tool": "claim", "args": {"paths": ["src/auth/token.rs"], "reason": "fix 401 storm when two requests refresh the access token concurrently"}},
      "expect": {"must_read": ["n1", "d1", "m1"]}
    },
    {
      "id": "smoke-brief-002",
      "difficulty": "hard",
      "why": "shared words (invoice, rounding) but the relevant row is a storage move",
      "seed": {
        "notices": [
          {"key": "n1", "by": "alice", "kind": "moved", "summary": "blob client moved from storage::s3 to storage::object; S3Client -> ObjectStore trait", "from": "storage::s3::S3Client", "to": "storage::object::ObjectStore", "affected_paths": ["src/billing/export.rs", "src/storage"]},
          {"key": "n2", "by": "carol", "kind": "behavior", "summary": "invoice totals rounding switched to banker's rounding", "from": null, "to": null, "affected_paths": ["src/billing"]}
        ],
        "decisions": [
          {"key": "d1", "by": "carol", "title": "Invoice rounding", "decision": "use banker's rounding for invoice line totals", "rationale": "matches the ledger", "affects_paths": ["src/billing"]}
        ],
        "memory": [
          {"key": "m1", "by": "erin", "title": "CSV export must stream", "kind": "lesson", "body": "Exports over 50k invoices OOM'd when buffered; write rows through the ObjectStore multipart writer.", "paths": ["src/billing/export.rs"], "tags": ["billing", "export"]},
          {"key": "m2", "by": "dave", "title": "Invoice PDF fonts", "kind": "fact", "body": "PDF invoices embed Inter; licensing cleared.", "paths": ["src/billing/pdf.rs"], "tags": []}
        ]
      },
      "action": {"agent": "bob", "tool": "claim", "args": {"paths": ["src/billing/export.rs"], "reason": "upload monthly invoice CSV export to object storage"}},
      "expect": {"must_read": ["n1", "m1"]}
    }
  ]
}
