If `claim` returns `conflict`, do not edit those paths: its `conflicts` field says who holds what and why. Wait in the foreground with `python3 -c "import time; time.sleep(30)"` and claim again (a foreground loop of claims and sleeps until `ok` is fine), or work on another part of your task first. Message the holder with `tb message_send` only after you have waited more than 5 minutes for the same paths.
