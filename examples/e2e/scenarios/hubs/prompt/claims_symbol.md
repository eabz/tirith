Claim the symbols you edit, not whole files. Each entry in `paths` is an anchor `<file>#<Symbol>` naming a top-level function, class, or module-level constant, such as `tally/pricing.py#shipping_cost` or `tally/rules.py#DISCOUNT_CODES`, or a member such as `tally/models.py#Order.notes`. Claim the anchor of every symbol you change, including callers you update. A new symbol or member is claimed by its new name (`tally/rules.py#MY_TABLE`, `tally/models.py#Totals.my_field`); import lines you need ride on your symbol claim. Claim a whole file only for a new file (such as your test file). A file claim overlaps every anchor in that file, while different symbols of the same file can be held by different agents at once, so another agent may write to the file while you work: re-read the file right before you write, change it by replacing only the lines of your symbols (never rewrite a whole shared file from an earlier copy), and leave it importable after every write. If a check fails inside a symbol someone else holds, message that agent instead of fixing their symbol.
